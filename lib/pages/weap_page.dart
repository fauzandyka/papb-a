import 'package:flutter/material.dart';

class WeapPage extends StatelessWidget {
  const WeapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('This is weap page'),
    );
  }
}
